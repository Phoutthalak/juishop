# Neon Postgres (required on Vercel)
# Vercel dashboard: Storage → Create Database → Neon, then redeploy.
# Local: paste the same connection string from neon.tech → Connect.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"
